# Database Migrations

- Manual review-first migration policy.
- Plan → SQL Draft → Schema Review → Apply Dev DB → Verify → Implement → Smoke → Audit → Commit.
- No migration apply without explicit approval.

## Migration History
- `202607060001_zamblak_core_schema.sql` (Applied): Core schema foundation containing 10 tables, triggers, helper functions, and views. Applied manually on target `gdegnwglakyblnmxgiwx` via Supabase SQL Editor inside explicit `BEGIN/COMMIT`. Hardened via ZAMBLAK-MIGRATION-001-SQL-FIX-2, FIX-3, FIX-4 (fail-closed SECURITY DEFINER triggers), FIX-5 (safeguarded OLD.review_status trigger evaluation), and FIX-6 (current_profile_role renaming).
- `202607130001_participation_project_state_guard.sql` (Applied): Participation membership project-state guard. Committed/pushed in `0d48fe8ed2d67b2f923a44b59f52a6dab0010577`, manually applied to designated DEV/DEMO `gdegnwglakyblnmxgiwx`, and post-apply verified. Supabase migration-history registration is not claimed.
- `202607130002_role_safe_read_surfaces.sql` (Applied, DEV/DEMO): Role-safe owner and support-helper read surfaces under program `ZAM-WF-001F-RLS-READ-SURFACE`. Frozen snapshot SHA-256 `AE01C67A188E188533769E946C4965878975381B0E5CB1AF750431028F56EC8D` was manually applied once by Mozfer to designated DEV/DEMO `gdegnwglakyblnmxgiwx` with no SQL error. Read-only post-apply verification passed (`overall_verdict = PASS`) for 11 managed functions, 2 managed views, 23 managed policies, core-table SELECT ACLs, preserved project-state guard, unique respondent-per-project index, and expected RLS state. Managed manifest MD5 `f950c7ec5024dcf907d36f02df8c78b4` (8238 octets). Local verification-packet formatting corrections were required before the final evidence-exact PASS; those were packet issues, not database defects. Supabase migration-history registration, browser smoke, live application authorization, and customer production readiness remain unclaimed.
- `20260714114814_first_owner_bootstrap.sql` (Applied, DEV/DEMO): Globally one-time first-Owner bootstrap function under `ZAM-AUTH-001C`. Committed with `current_user` syntax fix through `fd847f5`. Mozfer manually applied the full `BEGIN/COMMIT` script once via SQL Editor on designated DEV/DEMO `gdegnwglakyblnmxgiwx` as `postgres`. Post-apply verification: function owner `postgres`, `plpgsql`, `SECURITY DEFINER`, search path `pg_catalog, public`, EXECUTE revoked for PUBLIC/anon/authenticated/service_role, frozen lock and `auth.users` checks present. One-time bootstrap completed (account `Zamblak Field Research`; one active Owner; Support Helpers `0`; replay `bootstrap_already_completed`). Path consumed on that database. Supabase migration-history registration, live app login/session, browser smoke, and customer production readiness remain unclaimed.
- `20260715120000_harden_core_acl_defaults.sql` (Applied, DEV/DEMO): Core table/view ACL least-privilege hardening, named trigger-function EXECUTE hardening, and `postgres` GLOBAL + `IN SCHEMA public` default-privilege hardening under program `ZAM-SEC-ACL-001`. Committed and pushed as `846894e fix(security): harden core database ACLs` (`846894ed3ccfad21545a5f9e451814dbc8edfa16`). Mozfer manually applied the full `BEGIN/COMMIT` script once via SQL Editor on designated DEV/DEMO `gdegnwglakyblnmxgiwx` as `postgres` (PostgreSQL 17.6). Six-section post-apply catalog verification passed: authenticated SELECT-only on 10 core tables + 2 managed views; anon/service_role no relation privileges; RLS still enabled; 10 trigger functions client EXECUTE false; 22/22 trigger attachments preserved; nine approved helpers/RPCs authenticated-only; internal helpers and bootstrap non-client-callable; no remaining client/PUBLIC `postgres` GLOBAL or public default grants for tables/sequences/functions. `supabase_admin` default ACLs intentionally out of scope. Owner smoke PASS (login, session, shell, placeholders). Support Helper runtime smoke not tested (no account); deferred P2/nonblocking because support RPC EXECUTE contracts were catalog-verified. No rollback required. Supabase migration-history registration and customer production readiness remain unclaimed.
- `20260716120000_companies_mvp_schema_rpc.sql` (Applied, DEV/DEMO): Companies MVP schema enforcement and bounded RPCs under `ZAM-COMPANIES-001`. Source committed as `f503c7ef feat(companies): add schema enforcement and RPCs`. First DEV/DEMO apply failed with **42P13** (required `p_expected_updated_at` after defaulted parameters on `update_company`); error before COMMIT; transaction rolled back; no partial apply. Parameter order corrected and pushed as `6acc2e34 fix(companies): correct update RPC parameter order` (`6acc2e346ab0f3314f928ed1e5c3cef24462daab`). Mozfer re-applied the full corrected `BEGIN/COMMIT` script once via SQL Editor on designated DEV/DEMO `gdegnwglakyblnmxgiwx` as `postgres` (PostgreSQL 17.6) with no SQL error; advisory lock row returned as expected; in-script postconditions passed; transaction committed. Manual eight-object verification all true: name/phone helpers, four RPCs (`list_companies`, `get_company`, `create_company`, `update_company` with identity `uuid,text,timestamp with time zone,text,text,text`), `idx_companies_account_norm_name_active`, `idx_projects_account_company_status_live`. No corrective migration required (same filename). Application/browser smoke, Owner/Support Helper runtime RPC smoke, Supabase migration-history registration, and production readiness remain unclaimed.
- `20260716160000_projects_mvp_schema_rpc.sql` (Applied, DEV/DEMO; later corrected): Projects MVP consistency harden + five product RPCs under `ZAM-PROJECTS-001`. Source committed as `1cb9a75e feat(projects): add schema enforcement and RPCs` (`1cb9a75e3eb5ee9d88dbf3e59011a1bf2b12d9f5`). Mozfer applied once via SQL Editor on designated DEV/DEMO `gdegnwglakyblnmxgiwx` as `postgres` (PostgreSQL 17.6); result **Success. No rows returned**. Initial metadata post-apply verification **HOLD**: (1) `list_projects` search length `>120` raised `invalid_project_pagination` instead of `invalid_project_text_length`; (2) `transition_project_status` validated parent Company with `SELECT EXISTS` without row lock. Relation ACL/RLS non-regression and finance-free shapes otherwise PASS. Application contracts/UI and production readiness unclaimed.
- `20260716170000_projects_mvp_rpc_corrections.sql` (Applied, DEV/DEMO): Narrow correction of the two HOLD defects. Source committed as `dc03784d fix(projects): correct RPC validation and company locking` (`dc03784d2822a642de00af2df8481ccd1c792b0e`). Mozfer applied once on the same DEV/DEMO as `postgres`; result **Success. No rows returned**. Final narrow live verification **PASS**: search bound token fixed; transition Company `FOR SHARE` present; Project `FOR UPDATE` retained; lifecycle matrix and Owner-only transition preserved; authenticated-only EXECUTE; list/detail shapes unchanged; no finance exposure. Does not replace the initial migration file. Application/browser smoke, Supabase migration-history registration, and production readiness remain unclaimed.
